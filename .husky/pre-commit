#!/bin/sh
if git grep -q 'waitForTimeout' -- ':!*.md' ':!.husky/pre-commit'; then
  echo 'Error: waitForTimeout is forbidden. Use event-based waiting instead.'
  exit 1
fi

# Run E2E tests
echo "Running E2E tests..."
npx playwright test
