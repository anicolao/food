OK a very strange thing: all teh transitions seem to work fine when I run the dev server. But when I load the deployed code (which is a static site on github pages) only *some* of the transitions work. I can't think why this should be. All transitions are meant to be driven by a simple priority function that decides what to do, which I thought would work exactly the same in these two scenarios But it doesn't.
