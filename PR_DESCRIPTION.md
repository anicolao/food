The service worker implementation currently passes all drive URLs through to drive. But photos ought to be cached by this layer: they never change, and require a lot of time to load, and are all broken if the user is offline. Write a PHOTOS_CACHE.md design doc and follow WORKFLOW.md to make a PR for its review, so that we can fix it.
